public interface ShapeInterface {
    /**
      * This method helps the user to SET UP the offset of the shape
      * which sets up the coordinates of the shape's location
      * @param offset
      */
            public void setOffset(int offset);

    /**
      * This RETURNS the offsets/coordinates of the shape
      * @return offset - an integer
      */
            public int getOffset();

    /**
      * this method SETS the color of the shape
      * @param color
      */
            public void colorShape(String color);


}

